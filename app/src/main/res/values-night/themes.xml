<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark mode theme — phải match tên theme trong values/themes.xml -->
    <style name="Theme.AIStudyMentor" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/primary_dark</item>
    </style>

    <!-- Splash screen dark mode -->
    <style name="Theme.AIStudyMentor.NoActionBar" parent="Theme.AIStudyMentor">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@color/primary_dark</item>
    </style>
</resources>
